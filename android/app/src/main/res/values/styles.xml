<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Was `?android:colorBackground`, which resolves against this style's
             OWN parent (Theme.Light), i.e. white, regardless of the app's
             actual (dark) canvas. LaunchTheme above correctly shows the dark
             launch_background drawable while the process starts, but the
             instant Flutter's engine attaches and switches the window to
             THIS theme (still before Flutter's own first frame paints), the
             background flashed white on any device where the SYSTEM (not
             in-app) theme is light: the majority of phones by default,
             independent of whatever theme the user picked inside AFOS
             itself. Using the same dark drawable here removes the seam
             entirely: LaunchTheme, then NormalTheme, then Flutter's first
             frame are now all the identical dark canvas colour. -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
</resources>
