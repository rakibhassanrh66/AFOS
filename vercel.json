{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b 3.41.6 _flutter && _flutter/bin/flutter config --enable-web && _flutter/bin/flutter pub get && _flutter/bin/flutter build web --release --base-href /",
  "outputDirectory": "build/web",
  "framework": null,
  "rewrites": [{"source": "/(.*)", "destination": "/index.html"}],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {"key": "X-Content-Type-Options",   "value": "nosniff"},
        {"key": "X-Frame-Options",           "value": "DENY"},
        {"key": "X-XSS-Protection",          "value": "1; mode=block"}
      ]
    }
  ]
}
